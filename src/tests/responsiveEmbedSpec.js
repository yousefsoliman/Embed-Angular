describe('app: responsiveEmbed', function() {
    // Load angular module
    beforeEach(module('responsiveEmbed'));

    // Load angular controller
    var $controller = null;
    beforeEach(inject(function(_$controller_) {
        $controller = _$controller_;
    }));

    // Tests
});